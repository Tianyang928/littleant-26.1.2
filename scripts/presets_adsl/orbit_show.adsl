# A small action composition. It intentionally uses immediate body actions;
# no goal is submitted, so the sequence is not confused with synchronous goals.

@receive_goal
def show():
    say("orbit show")
    repeat(2):
        jump()
        rotate(180)
        step_forward(2)
        look_at_xyz(x(), y(), z())
